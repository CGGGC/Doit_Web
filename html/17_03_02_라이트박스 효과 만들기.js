function click_img() {
    // 이미지의 요소를 모두 가져옴
    let img = document.getElementsByClassName("get_img");
    // 라이트박스 영역의 요소를 가져옴
    let lightbox_img = document.getElementById("box_02");
    // 라이트박스 이미지 요소를 가져옴
    let lightbox = document.getElement
}